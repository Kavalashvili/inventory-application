extends layout

block content
  h1= title

  ul
    each brand in brand_list
      li
        a(href=brand.url) #{brand.brand_name}
        |  (#{brand.founded})

    else
      li There are no brands.
